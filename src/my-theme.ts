import { DefaultTheme } from 'styled-components'

const myTheme: DefaultTheme = {
  borderRadius: '5px',
  dark: true,
  colors: {
    main: 'cyan',
    secondary: 'magenta',
  },
}

export { myTheme }